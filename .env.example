# URL del backend (sin barra al final, sin /api — el cliente lo agrega)
# Desarrollo local:
VITE_API_URL=http://localhost:3000
# Producción (Railway): reemplazar con la URL pública de tu API en Railway
# VITE_API_URL=https://tu-api.up.railway.app
